import type { Post } from './types'

/**
 * All blog posts. Add `Post` objects here to list them on `/blog` and serve them at `/blog/[slug]`.
 * Keep slugs URL-safe (lowercase, hyphens, no spaces).
 */
export const posts: Post[] = []
